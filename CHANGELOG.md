# Changelog

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/).

## [0.2.15] — 2026-06-07

### Исправлено

- **Связь у классовых форм глагола**: `вуго` → `мужской род`, `руго` → `множественное число`, `йиго`/`ячӏина` → `женский род` — не «инфинитив» заголовка

## [0.2.14] — 2026-06-07

### Исправлено

- **Регистр в примерах**: `Хъахӏилаб` → `хъахӏилаб` при точном совпадении со словарём; без ложного маппинга имён (`Ахӏмад`); пост-объединение case-дубликатов

## [0.2.13] — 2026-06-07

### Добавлено

- **Классовый префикс глагола (б/в/р/й/я/е)**: `вуго` → `буго`, `вачӏина`/`рачӏина`/`ячӏина` → `бачӏине`, `йиго` → `буго`, `йокьана` → `бокьизе` и т.п. — только если б-форма есть в словаре у глагола

## [0.2.12] — 2026-06-07

### Добавлено

- **Страница происхождения** `source/?form=…` — клик по источнику в таблице открывает упоминания словоформы: подисточник, статья словаря, примеры av/ru, forms[]
- Экспорт provenance при сборке в `docs/data/sources/` (чанки по префиксу)

## [0.2.11] — 2026-06-07

### Исправлено

- **Страница статистики**: блок «Покрытие и пробелы» читает `gap_wordforms` / `gap_mentions` из `stats.json`; cache-bust для `app.js` / `stats.js` по `build_id`

### Изменено

- **Пост-объединение словоформ**: после извлечения немаппленные упоминания сливаются с единственной известной леммой для той же формы и источника (`тахитӏа` → одна строка `тах`, count 3)

## [0.2.10] — 2026-06-07

### Изменено

- **Скорость сборки**: ~40× быстрее (~5 с вместо ~3 мин) — `lru_cache` на `lookup_lemma`, индексы форм по первому символу/префиксу, кэш полей статьи, один проход для gap-статистики

## [0.2.9] — 2026-06-07

### Изменено

- **Кавычки в примерах `av`**: типографские `""` и `«»` снимаются с токенов (`"лъаларо"` → `лъаларо` → `лъазе`); дефис внутри слова не разрывает (`совхоз-колхоз`)

## [0.2.8] — 2026-06-07

### Исправлено

- **Заголовок словаря в приоритете**: токен, совпадающий с `word` основной статьи, всегда маппится на неё — префиксный поиск в контексте чужой статьи не применяется (`буго` ≠ `бугьналага`)

## [0.2.7] — 2026-06-07

### Изменено

- **Статистика пробелов**: на странице stats — **словоформы** (уникальные) и **упоминания** (сырые записи) разделены явно
- **Фильтры пробелов** больше не дублируют один набор: `needs_work` (4322 / 9286), `fully_unmapped` (4010), `partial` (312), `homograph` (1250), `strange` (0)
- Старые id фильтров (`missing_lemma`, …) перенаправляются на `needs_work`

## [0.2.6] — 2026-06-07

### Изменено

- **Чужие заголовки в `forms[]`**: форма, совпадающая с заголовком другой статьи, не привязывается к чужой лемме (`чӏвазе` у `гъветӏ`, `мал`)
- **Примерные корни в `av`**: если в примерах статьи есть более короткий токен, не объявленный в `forms[]`, не тянем словоформу к заголовку по слабому префиксу (`гьабихъа` ≠ `гьобо`)
- **Суффикс от чужой леммы**: короткий заголовок не матчится, если токен не в его парадигме; глобальный префикс — только при коротком «хвосте» после совпадения
- Бэклог пробелов: ~4322 словоформы (ранее ~1541 — многие записи были ложно «ясными»)

## [0.2.5] — 2026-06-07

### Изменено

- **Префиксное совпадение с формами** по всему словарю: формы вроде `килщида` сопоставляются с `килщил`/`килщица` → `килищ`, даже при орфографии заголовка `килищ`
- Бэклог пробелов: ~1541 словоформа (−1530)

## [0.2.4] — 2026-06-07

### Изменено

- **Маппинг в контексте статьи**: токены из примеров сопоставляются с заголовком по `stem` и общему префиксу с формами (напр. `диргъун` → `диргъизе` в статье `диргъизе`)
- Бэклог пробелов: ~3071 словоформа (−621)

## [0.2.3] — 2026-06-07

### Изменено

- **Фильтры пробелов** показывают только записи без уверенного контекста (~3692 словоформы), а не все с пустым полем
- Заголовки статей, `forms[]`, примеры с найденной леммой больше не попадают в «анomalies» и «без связи»
- «Аномalии» — только подозрительные записи без ясного источника (сейчас пусто)

## [0.2.2] — 2026-06-07

### Добавлено

- **Две ссылки у леммы**: колонка «Лемма» — словоформы этой леммы на forms (`?lemma=…`); колонка «avar.me» — словарная статья на dev.avar.me
- Индекс `lemma_index.json` при сборке сайта

## [0.2.1] — 2026-06-07

### Добавлено

- **Кликабельные пробелы на stats** — карточки «Покрытие и пробелы» ведут на главную с фильтром (`?filter=…`), показываются 30 случайных словоформ из пробела
- Фильтры пробелов генерируются при сборке (`output/gaps/` → `docs/data/gaps/`)

### Изменено

- **Маппинг по суффиксу**: в статье примера и по всему словарю — если форма отличается от известной только суффиксом (напр. `херасе` ← `херас`), лемма определяется автоматически
- **Часть речи** в примерах берётся только от найденной леммы, не от статьи, где встретился пример

## [0.2.0] — 2026-06-07

### Изменено

- **Все токены из примеров `av` попадают в корпус** — без фильтрации «не найдено в индексе»
- Маппинг lemma/relation по индексу и контексту статьи; если не найден — поля пустые
- Удалены хардкоды под отдельные слова (в т.ч. спецлогика для `буго`)
- Правила извлечения: `.cursor/rules/wordform-extraction.mdc`

## [0.1.3] — 2026-06-07

### Исправлено

- Спряжения связки **буго** (`вуго`, `руго`, `йиго`) — в словаре они только в примерах статьи, не в `forms[]`, поэтому раньше не попадали в корпус

## [0.1.2] — 2026-06-07

### Исправлено

- Леммы-заголовки (инфинитив, именительный и др.) получают самоссылку: `пуйзе → пуйзе (инфинитив)`, `квач → квач (именительный)` и т.д.
- Производные формы (`квачан → квачазе`) по-прежнему ссылаются на базовое слово

## [0.1.1] — 2026-06-07

### Изменено

- Леммы ведут на [dev.avar.me](https://dev.avar.me/) (`#word=…`) — перевод, примеры, полная статья
- Палитра сайта слегка смещена (шалфейный акцент вместо золотого) — отличие от словаря при общей тематике

## [0.1.0] — 2026-06-07

### Добавлено

- Пайплайн сборки словоформ: `./build.sh` → CSV, статистика, сайт
- Первый источник — **AV-RU словарь Гимбатова** (`sources/gimbatov.av-ru.enriched.jsonl`):
  - явные связи (`masdarfrom`, `pluralfor`, `genitivefrom`, `deverbfrom` …)
  - парадигмы из `forms[]` и родовые формы
  - токены из примеров `av` с матчингом по индексу словаря
- Фреймворк `avarforms/`: модели, экстракторы, агрегация, статистика
- Реестр источников `config/sources.json`, ручные маппинги `mappings/`
- Статический сайт в `docs/` (GitHub Pages):
  - **Поиск** — search-suggest по 47 547 словоформам, 10 случайных на главной
  - **Статистика** — графики и цифры по корпусу
- Домен **forms.avar.me** (CNAME → GitHub Pages)

### Цифры первой сборки

| Метрика | Значение |
|---------|----------|
| Сырых записей | 69 935 |
| Агрегированных строк CSV | 50 646 |
| Уникальных словоформ на сайте | 47 547 |
| Чанков для поиска | 644 |

[0.2.0]: https://github.com/avar-me/forms/compare/v0.1.3...v0.2.0
[0.1.3]: https://github.com/avar-me/forms/compare/v0.1.2...v0.1.3
[0.1.2]: https://github.com/avar-me/forms/compare/v0.1.1...v0.1.2
[0.1.1]: https://github.com/avar-me/forms/compare/v0.1.0...v0.1.1
[0.1.0]: https://github.com/avar-me/forms/releases/tag/v0.1.0
