# Запросы на исправление источников

Сгенерировано автоматически инструментом forms.avar.me на основе анализа аномалий в данных.
Каждый раздел — отдельный запрос соответствующим мейнтейнерам.

---

## 1. taras_bulba.json — Латиница вместо кириллицы

**Источник:** `data/taras_bulba.json`

**Проблема:** Во многих словах стоят визуально похожие латинские буквы вместо кириллических (гомоглифы). Вероятная причина — OCR или копирование из PDF с перемешиванием кодировок.

| Латинская | Кириллическая | Код |
|-----------|--------------|-----|
| `x` | `х` | U+0078 → U+0445 |
| `a` | `а` | U+0061 → U+0430 |
| `p` | `р` | U+0070 → U+0440 |
| `o` | `о` | U+006F → U+043E |
| `e` | `е` | U+0065 → U+0435 |
| `y` | `у` | U+0079 → U+0443 |
| `t` | `т` | U+0074 → U+0442 |

**Примеры затронутых словоформ:**
```
гӏapaкъидул   (a, p, a — латиница)  → гӏараракъидул
xӏaталдаса    (x, a — латиница)     → хӏаталдаса
xӏypматалъе   (x, y, p — латиница)  → хӏурматалъе
тӏoтӏoлаго   (o, o — латиница)     → тӏотӏолаго
гӏeмераб      (e — латиница)        → гӏемераб
бетӏtраб      (t — латиница)        → бетӏираб
зáмокал       (á U+00E1 — не кириллица) → русское слово с ударением?
```

**Рекомендация:** Автозамена гомоглифов по всему файлу + проверка откуда попали русские слова с `á` (зáмокал, оргáналъул).

---

## 2. av-ru.jsonl — Двойной слеш в примерах

**Источник:** `data/av-ru.jsonl`

**Проблема:** В текстах примеров используется `//` для разделения вариантов. Это нестандартно — вариативные формы должны быть отдельными примерами или размечены иначе.

**Примеры:**
```
бецӏлъуда//бецӏлъукь
нухалда//нухалъ
```

**Рекомендация:** Разбить на отдельные примеры или заменить `//` на ` / ` с пояснением.

---

## 3. en-av.jsonl — Опечатки и спецсимволы

**Источник:** `data/en-av.jsonl`

**Проблема 1:** Латинская `h` вместо кириллической `х`:
```
наhасан  →  нахасан  (h U+0068 вместо х U+0445)
```

**Проблема 2:** Слеш разделяет варианты в одном поле:
```
нахъа-фон/нахъа-бахъ
```

**Проблема 3:** Диакритический знак U+0306 (краткая) на букве:
```
байӑ-рахъалде  (содержит U+0306 после а)
```

**Рекомендация:** Исправить `h` → `х`, разбить варианты через `/` на отдельные поля, убрать лишний диакритик.

---

## 4. ru-av.jsonl — Спецсимволы и точки внутри словоформ

**Источник:** `data/ru-av.jsonl`

**Проблема 1:** Точки и сокращения внутри аварских слов (видимо, сокращения из оригинала попали в словоформы):
```
ретӏел.цӏунулеб
ц.балеб
вагь.ц.хӏалтӏизабулеб
вагь.ц.компютералде
и.гончаровасул
кого-л.лъихъниги
```

**Проблема 2:** Слеш в словоформах:
```
лъиданиги/сунданиги
```

**Проблема 3:** Тильда и обратный слеш:
```
~гьеб
киназд\даго
```

**Проблема 4:** Цифры слиты с аварскими окончаниями без дефиса:
```
1ччараб
6данде
```
(сравни норму: `1905-абилеб`, `15-ялде` — с дефисом)

**Проблема 5:** 21 словоформа начинается с голой палочки `ӏ` без предшествующей согласной. Возможно, в источнике пропущена буква (например, `хӏ-`):
```
ӏахал       ӏакимлъи     ӏибитӏизе
ӏулараб     ӏалтӏуца     ӏого
ӏунчизе     ӏолареб      ӏехьезе
ӏияхъанлъиялъул  ӏеб   ӏаде
ӏагарлъи    ӏал          ӏаллъи
ӏезе        ӏадатго      ӏакъикъаталдаги
ӏеренго     ӏага-шагарго ӏ-себ
```

**Рекомендация:** Проверить каждую категорию; сокращения и слеши указывают на то, что словоформы скопированы из словарной статьи без очистки.

---

## Что НЕ является ошибкой

Для справки — следующие паттерны встречаются в корпусах и являются нормой:

- `1990-абилел`, `15-ялде`, `100-гӏанасеб` — числа с аварскими падежными окончаниями через дефис
- Слова с дефисом: `хӏал-хӏасилалъ`, `цо-цо`
- Двойные слова через дефис

---

*Сформировано: forms.avar.me | Источник аномалий: `output/stats.json` → `strange_records`*
